<style>
@keyframes awaitedFadeOut {
  99% {
    opacity: 0;   
    display: block;
  }
  100% {
      display: none;
  }
}

  div {
    position: relative; 
  }

  :global([data-fallback])  {
    opacity: 1;
  }

  div:only-child > :global([data-fallback]) {
      position: absolute;
      top: -1rem;
      left: 0;
      width: 100%;
      height: 100%;
      animation: awaitedFadeOut .125s linear forwards;
  }
</style>
<div class="">
  <slot name="fallback" />
  <slot />
</div>
