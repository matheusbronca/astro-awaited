
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Integration Test</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				max-width: 800px;
				margin: 0 auto;
				padding: 2rem;
				line-height: 1.6;
			}
			
			h1 {
				color: #333;
				border-bottom: 2px solid #007acc;
				padding-bottom: 0.5rem;
			}
			
			h2 {
				color: #555;
				margin-top: 2rem;
			}
			
			.test-section {
				background: #f5f5f5;
				padding: 1.5rem;
				border-radius: 8px;
				margin: 1rem 0;
			}
			
	    /* A trick to force our fake footer goes below the main content */
		  [data-footer-fallback] {
		    order: 3;
		  }
		  
		  
		  /* When the real footer is finally streamed, we hide our fallback, immediately; */
		  body:has(footer) [data-footer-fallback] {
		    display: none;
		  }

			footer {
				margin-top: -2px;
			}
		</style>
	</head>
	<body style="display: flex; flex-direction: column;">
		<!-- We should always provide a fallback of the footer and sidebars above the main, -->
		<!-- This way, we'll be sure that they will be always available during HTML streaming; -->
		<div data-footer-fallback aria-hidden="true">
			<h2>Expected Behavior</h2>
			<p>
  			The <code>{'Fallback'}</code> component above renders a placeholder while data is being fetched. Once the data is available, the placeholder is automatically replaced with the actual content.
			</p>
		</div>
	  
	  <main>
	    <slot />
	  </main>
    <footer>
			<h2>Expected Behavior</h2>
			<p>
  			The <code>{'Fallback'}</code> component above renders a placeholder while data is being fetched. Once the data is available, the placeholder is automatically replaced with the actual content.
			</p>
    </footer>
	</body>
</html>
